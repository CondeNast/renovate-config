{
  "description": "Groups minor dependency updates for non-condenast packages.",
  "packageRules": [
    {
      "packagePatterns": ["*"],
      "excludePackagePatterns": ["@condenast/*"],
      "minor": {
        "groupName": "{{arg0}}"
      }
    }
  ]
}
