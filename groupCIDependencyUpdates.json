{
  "description": "Groups all the CI pipeline dependency updates together.",
  "packageRules": [
    {
      "managers": ["circleci"],
      "groupName": "CI dependencies",
      "patch": {
        "groupName": "CI dependencies"
      },
      "minor": {
        "groupName": "CI dependencies"
      },
      "pin": {
        "groupName": "CI dependencies"
      },
      "digest": {
        "groupName": "CI dependencies"
      },
      "lockFileMaintenance": {
        "groupName": "CI dependencies"
      }
    }
  ]
}
